[project]
name = "agent-company-simulator-backend"
version = "0.1.0"
description = "Agent Company Simulator backend (Phase 1 foundation)"
requires-python = ">=3.11"

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
addopts = "-q"
