# Optional: base URL for the backend API.
# The default dev setup proxies /api to http://localhost:8000 via vite.config.ts,
# so this file is only needed to override the target.
VITE_API_BASE=http://localhost:8000
